<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/apphead.ejs %>
    <!--
      confirm that this is the inital page on local port 5000
      -->
</head>

<body>
    <div id="container">

        <div id="header">
            <h3 id="title">Not A Web App</h3>

            <br>
            <form id="loginForm" method="post">
                <input type="text" name='userid' placeholder="user id" maxlength="25" required>
                <input type="password" name='password' maxlength="25" placeholder="password" required>
                <br>
                <input type="submit" id="login" onclick="loginUser()" value="login"></button>
            </form>
            <h5 id="msg"></h5>
            <h6>don't have an account?</h6>
            <a href="signup"> >> Sign Up << </a>
        </div>


    </div>
    <!--
    <form id="test" method="post">

        GET/POST using forms here because the alternative XLM/PHP methods are nightmares to use
        and frankly I think it's outside of the scope of the course for a 2-3 person project
        not sure if this can be made more complex with use of stock buttons over (submit) buttons

      <input type="submit" onclick="getPage()" value="short"></button>
      -->
      
    </form>
    <script>
        //Login user
        function loginUser() {
          document.getElementById('loginForm').action = '/login';
          document.getElementById('loginForm').submit();
        }
        function getPage() {
          document.getElementById('test').action = '/socket'
          document.getElementById('test').submit()
          
        }
    </script>
</body>

</html>